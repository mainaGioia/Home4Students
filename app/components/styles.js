import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  list: {
    flex: 1,
  },
  title:{
    fontSize: 34,
    textAlign: 'center',

  },
  subtitle: {
    fontSize: 24,
    textAlign: 'center',
    paddingTop: 10,
  }
});
